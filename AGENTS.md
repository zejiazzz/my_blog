# 项目协作机制

## 智能多角色分配规则

根据任务复杂度和类型，自动分配合适的 agent 角色参与协作：

### 可用角色
- **产品 agent**：需求分析、功能设计、用户体验规划
- **前端 agent**：UI 实现、交互逻辑、组件开发
- **后端 agent**：API 设计、数据处理、服务端逻辑
- **UI agent**：视觉设计、样式实现、响应式布局
- **测试 agent**：测试用例、质量验证、回归测试
- **运维 agent**：部署配置、性能优化、监控告警

### 分配原则
1. **简单对话/咨询**：无需启动 agent，主 agent 直接回答
2. **单一技术问题**：启动 1 个对应角色的 agent
3. **功能开发**：根据需求复杂度启动 2-3 个 agent（如：产品 + 前端，或产品 + 前端 + 测试）
4. **完整需求**：启动 3-5 个 agent，产品 agent 必须参与需求完善

### 工作流程
1. 收到需求后，主 agent 先评估复杂度和涉及领域
2. 如需启动 agent，产品 agent 优先介入完善需求
3. 其他 agent 基于完善后的需求并行工作
4. 主 agent 负责协调、整合和最终输出

## 部署约定

- 本项目以 Git 远程提交触发部署。
- 修复或功能完成后，如需发布，直接提交并推送远程代码，不需要额外手动触发部署。
- 推送前仍需完成最相关的本地验证，并在最终回复中明确说明验证结果与未验证项。

### 示例场景
- "修改按钮颜色" → 无需 agent，直接处理
- "优化首页加载速度" → 前端 agent + 运维 agent
- "新增用户评论功能" → 产品 agent + 前端 agent + 后端 agent + 测试 agent
- "重构认证系统" → 产品 agent + 前端 agent + 后端 agent + 测试 agent + 运维 agent
